<?xml version="1.0" encoding="utf-8"?>
<odoo noupdate="0">

    <record id="sale.sale_order_personal_rule" model="ir.rule">
        <field name="name">Personal Orders Custom</field>
        <field ref="sale.model_sale_order" name="model_id"/>
        <field name="domain_force">['|',  '|', ('user_id','=',user.id), ('support_user_id','=',user.id), ('user_id','=',False)]</field>
        <field name="groups" eval="[(4, ref('sales_team.group_sale_salesman'))]"/>
    </record>

    <record id="sale.sale_order_line_personal_rule" model="ir.rule">
        <field name="name">Personal Order Lines Custom</field>
        <field ref="sale.model_sale_order_line" name="model_id"/>
        <field name="domain_force">['|', '|', ('order_id.support_user_id','=',user.id), ('salesman_id','=',user.id),('salesman_id','=',False)]</field>
        <field name="groups" eval="[(4, ref('sales_team.group_sale_salesman'))]"/>
    </record>

</odoo>
