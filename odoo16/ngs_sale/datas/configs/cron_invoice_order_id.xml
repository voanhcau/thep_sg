<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Cron job để fix các invoice thiếu invoice_order_id -->
        <!-- <record id="cron_fix_missing_invoice_order_id" model="ir.cron">
            <field name="name">Fix Missing Invoice Order ID</field>
            <field name="model_id" ref="account.model_account_move"/>
            <field name="state">code</field>
            <field name="code">model.cron_fix_missing_invoice_order_id()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="numbercall">-1</field>
            <field name="active">True</field>
            <field name="doall">False</field>
            <field name="nextcall" eval="DateTime.now() + timedelta(hours=1)"/>
            <field name="user_id" ref="base.user_admin"/>
            <field name="priority">5</field>
        </record> -->
    </data>
</odoo>
